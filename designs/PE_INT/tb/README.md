# tb（pyCircuit / 模型測試）

本目錄只放：

- pyCircuit testbench（例如 `tb_pe_int_pycircuit.py`）
- pyCircuit 相關測試流程檔

## 不放內容

- 模型本體與模型回歸（統一放 `model/`）
- RTL 專用 Verilog testbench
- RTL case 資料集

上述內容分別統一放在 `model/` 與 `tb_rtl/`。
