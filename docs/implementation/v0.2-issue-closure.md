# Queue/Var v0.2 Issue Closure Matrix

This matrix tracks executable evidence for Issues #9, #10, and #11. `Complete`
means the current repository contains direct tests for the stated requirement.
`Partial` means a supported subset exists but the issue's full acceptance scope
is not yet proven. This document intentionally keeps incomplete work visible.

## Issue #9 — Serial Python frontend

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Byte-identical ACPy/ACIR across roots, hash seeds, and processes | Complete | `tests/python_frontend/test_determinism.py`, repeated Queue lowering tests |
| No explicit module ports or Queue wiring in the complete example | Complete | `examples/v02/davincioo_queue_model.py` |
| Scope I/O, collection shape, and payload layout inference | Complete | `test_queue_frontend_v02.py`, `test_queue_codegen_v02.py` |
| Exact Queue/Var immutable type and effect checks | Complete | `test/ACIR/*-v0.2.mlir`, Queue/Var verifier unit tests |
| Statement atomicity and `with ac.atomic()` | Complete | frontend positive/negative tests plus gfsim atomic permutation tests |
| Implicit strict broadcast and explicit decoupled fork | Complete | frontend, ACIR, gfsim, and PYC tests |
| Runtime conditional and bounded feedback lowering | Complete | conditional and feedback Python/PYC/gfsim E2E tests |
| Runtime `break`/`continue` and compile-time recursion expansion | Complete | leading break and tail continue lower through bounded feedback; structurally decreasing Queue helpers expand at compile time with a 1024-stage bound |
| Runtime Queue collection indexing via select/route/arbitrate | Complete | flat static collections lower through official `ac.select`; PYC emits a selector mux and selected ready path without dynamic Queue pointers |
| Python-level `peek`/`pop`/`push` firing authoring | Complete | `Queue.firing` validates exactly one pop/push with repeated immutable peeks and normalizes to the standard atomic transform; raw ACIR effects retain precise verifier coverage |
| Dynamic topology, mutation, illegal comb cycles, and user opcodes rejected | Complete | frontend rejection corpus plus backend-entry QueueGraph producer/consumer/reference/cycle verification |
| No `ac.std.*` or v0.1 compatibility surface | Complete | epoch hard-break contract and repository scans |
| Every public Python API has success/error/determinism/machine-output evidence | Complete | public-surface rejection tests plus Queue/Var success/error/determinism and native artifact E2E coverage, including firing, loop control, recursion, select, credit, barrier, memory, and expect |

## Issue #10 — ACIR, optimization, and typed gfsim

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Closed v0.2 ODS, verifier, parser/printer, schema, and catalog | Complete | ACIR lit corpus, `schemas/opcodes-v0.2.json`, `acir-opcode-catalog` |
| Positive/negative/round-trip coverage for every official opcode | Complete | ACIR lit coverage ledger; current catalog has 17 graph-level blocks |
| Static array/map/set Queue/Var collections | Complete | type verifier and serial frontend canonical flattening tests |
| Atomic firing independent of legal Work permutation | Complete | gfsim Queue block and dispatch-order tests |
| Proposal/Xfer, capacity, latency, observations, deadlock diagnostics | Complete | queue/runtime gates, static unconsumed/cycle checks, and generated barrier `no_progress` E2E diagnostic |
| Generated source/manifests byte-identical across roots | Complete | Queue codegen and CLI determinism tests |
| DavinciOO 15-record/opcode/retirement/result/453-cycle gate | Complete | `davincioo-softmax-projection.json` and Queue codegen E2E run both the oracle and generated model |
| DavinciOO queue/ROB occupancy and internal event-flow equivalence | Complete | checked projection fixes dependency-window peak 8, per-resource executing peaks `[1,1,0,1]`, reorder-window peak 8, completion/retirement order, and the 453-cycle oracle |
| Explicit dependency, reservation, credit, barrier, memory, and ROB building blocks | Complete | `ac.dependency` provides bounded resource reservation, `ac.credit` provides parallel in-flight completion, `ac.barrier` provides atomic synchronization, `ac.memory` provides typed old-data state, and `ac.reorder` provides retirement |
| Debug, Release, ASan, UBSan, lit, CTest, install consumer | Complete | [v0.2 final audit](v0.2-final-audit.md): Debug/Release 124 lit + 14 CTest, focused ASan/UBSan runtime/codegen suites, installed-tree consumer, and reference exclusion passed |
| Reference snapshot excluded from install | Complete | installation contract tests |

## Issue #11 — PYC C++ and Verilog refinement

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Every design-role official opcode has PYC lowering | Complete | shared catalog enforcement and PYC E2E tests, including `ac.barrier`, `ac.credit`, `ac.memory`, and `ac.reorder` |
| Stable packed struct/class layout | Complete | struct PYC/Verilog layout tests |
| Queue, Var, atomic, feedback, route/arbitrate, barrier, credit, memory, dependency, reorder lowering | Complete | targeted PYC C++/Verilator generation and cycle-equivalence tests |
| Array/map/set lowering | Complete | canonical static flattening plus runtime flat-collection `ac.select` PYC C++/Verilator equivalence |
| Runtime Queue selection becomes static mux/route/arbitrate | Complete | runtime-if and route/merge PYC tests |
| Verification/observation role boundaries | Complete | official verification-role `ac.expect` runs non-consuming in gfsim and is deterministically rejected from PYC design hierarchy with testbench-boundary guidance; `ac.observe` remains non-state-changing |
| Canonical artifact independently accepted by pinned `pycc` | Complete | `ac-queue-pyc-build.py` and locked toolchain manifest |
| PYC C++ and Verilog cycle-level equivalence | Complete | feedback, reorder, and 15-record DavinciOO tests |
| gfsim versus PYC/Verilog refinement observations | Complete | shared DavinciOO projection proves transactions and occupancy; barrier proves atomic positional synchronization; credit proves completion-order semantics; typed memory proves old-data semantics; verification-role expect has an explicit testbench boundary |
| Verilog lint, compile, and Verilator smoke | Complete | PYC bundle and E2E tests |
| Unrelated-root/repeated-build/replay byte identity | Complete | expanded barrier/credit/firing/loop/memory/recursion/select corpus compares complete PYC, C++, Verilog, primitive, profile, and manifest artifact sets byte-for-byte across independent output roots |
| No undeclared embedded pyCircuit runtime | Complete | process boundary, toolchain lock, build manifest, and install tests |

## Closure

All acceptance rows are complete. The exact gate evidence and local sanitizer
environment note are recorded in the [v0.2 final audit](v0.2-final-audit.md).
