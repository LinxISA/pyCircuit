# Queue/Var v0.2 Issue Closure Matrix

This matrix tracks executable evidence for Issues #9, #10, and #11. `Complete`
means the current repository contains direct tests for the stated requirement.
`Partial` means a supported subset exists but the issue's full acceptance scope
is not yet proven. This document intentionally keeps incomplete work visible.

## Issue #9 — Serial Python frontend

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Byte-identical ACPy/ACIR across roots, hash seeds, and processes | Complete | `tests/python_frontend/test_determinism.py`, repeated Queue lowering tests |
| No explicit module ports or Queue wiring in the complete example | Complete | `examples/v02/davincioo_queue_model.py` |
| Scope I/O, collection shape, and payload layout inference | Complete | `test_queue_frontend_v02.py`, `test_queue_codegen_v02.py` |
| Exact Queue/Var immutable type and effect checks | Complete | `test/ACIR/*-v0.2.mlir`, Queue/Var verifier unit tests |
| Statement atomicity and `with ac.atomic()` | Complete | frontend positive/negative tests plus gfsim atomic permutation tests |
| Implicit strict broadcast and explicit decoupled fork | Complete | frontend, ACIR, gfsim, and PYC tests |
| Runtime conditional and bounded feedback lowering | Complete | conditional and feedback Python/PYC/gfsim E2E tests |
| Runtime `break`/`continue` and compile-time recursion expansion | Missing | frontend currently accepts only the single-rebind bounded `while` form |
| Runtime Queue collection indexing via select/route/arbitrate | Missing | collection shape and static indexing are implemented; dynamic Queue indexing is rejected |
| Python-level `peek`/`pop`/`push` firing authoring | Partial | ACIR firing/effect contract exists; high-level Python statements do not yet expose the complete effect surface |
| Dynamic topology, mutation, illegal comb cycles, and user opcodes rejected | Partial | topology/mutation/user-opcode rejection exists; full QueueGraph combinational-cycle verification remains |
| No `ac.std.*` or v0.1 compatibility surface | Complete | epoch hard-break contract and repository scans |
| Every public Python API has success/error/determinism/machine-output evidence | Partial | core Queue/Var API is covered; closure must be re-audited after the remaining control/effect APIs land |

## Issue #10 — ACIR, optimization, and typed gfsim

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Closed v0.2 ODS, verifier, parser/printer, schema, and catalog | Complete | ACIR lit corpus, `schemas/opcodes-v0.2.json`, `acir-opcode-catalog` |
| Positive/negative/round-trip coverage for every official opcode | Complete | ACIR lit coverage ledger; current catalog has 13 graph-level blocks |
| Static array/map/set Queue/Var collections | Complete | type verifier and serial frontend canonical flattening tests |
| Atomic firing independent of legal Work permutation | Complete | gfsim Queue block and dispatch-order tests |
| Proposal/Xfer, capacity, latency, observations, deadlock diagnostics | Partial | queue/runtime gates are present; a complete generated-model deadlock corpus remains |
| Generated source/manifests byte-identical across roots | Complete | Queue codegen and CLI determinism tests |
| DavinciOO 15-record/opcode/retirement/result/453-cycle gate | Complete | `davincioo-softmax-projection.json` and Queue codegen E2E run both the oracle and generated model |
| DavinciOO queue/ROB occupancy and internal event-flow equivalence | Partial | dependency wakeup, per-resource reservation, and retirement order are explicit; per-unit issue/ROB occupancy projection remains |
| Explicit dependency, reservation, memory, and ROB building blocks | Partial | `ac.dependency` includes bounded resource reservation, `ac.memory` provides typed old-data state, and `ac.reorder` provides retirement; credit/barrier inventory remains |
| Debug, Release, ASan, UBSan, lit, CTest, install consumer | Partial | normal build/lit/CTest/install gates are green; fresh release and sanitizer evidence is still required |
| Reference snapshot excluded from install | Complete | installation contract tests |

## Issue #11 — PYC C++ and Verilog refinement

| Acceptance requirement | Status | Current evidence / remaining work |
| --- | --- | --- |
| Every design-role official opcode has PYC lowering | Complete | shared catalog enforcement and PYC E2E tests, including `ac.memory` and `ac.reorder` |
| Stable packed struct/class layout | Complete | struct PYC/Verilog layout tests |
| Queue, Var, atomic, feedback, route/arbitrate, memory, dependency, reorder lowering | Complete | targeted PYC C++/Verilator generation and cycle-equivalence tests |
| Array/map/set lowering | Partial | statically flattened Queue collections work at the frontend; explicit PYC collection artifacts need closure tests |
| Runtime Queue selection becomes static mux/route/arbitrate | Complete | runtime-if and route/merge PYC tests |
| Verification/observation role boundaries | Partial | observation is non-state-changing; verification-only leaf placement is not yet represented in the graph catalog |
| Canonical artifact independently accepted by pinned `pycc` | Complete | `ac-queue-pyc-build.py` and locked toolchain manifest |
| PYC C++ and Verilog cycle-level equivalence | Complete | feedback, reorder, and 15-record DavinciOO tests |
| gfsim versus PYC/Verilog refinement observations | Complete for current blocks | shared DavinciOO projection proves output transactions; typed memory proves common old-data semantics; future credit/barrier blocks need additional observations |
| Verilog lint, compile, and Verilator smoke | Complete | PYC bundle and E2E tests |
| Unrelated-root/repeated-build/replay byte identity | Partial | repeated canonical artifacts are tested; full replay evidence for the expanded inventory remains |
| No undeclared embedded pyCircuit runtime | Complete | process boundary, toolchain lock, build manifest, and install tests |

## Next closure order

1. Add explicit general-state, credit, and barrier blocks needed to close the
   remaining DavinciOO occupancy projection.
2. Add QueueGraph cycle/deadlock verification and generated-model negative
   tests.
3. Close dynamic Queue collection selection and Python firing effects.
4. Close verification-role placement and explicit PYC collection evidence.
5. Run the final Release/ASan/UBSan/install/replay audit before merging.
